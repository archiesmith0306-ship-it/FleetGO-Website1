services:
  - type: static-site
    name: fleetgo-website
    environment: node
    buildCommand: pnpm install && pnpm run build
    publishPath: ./dist
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
